# Hoist all dependencies to the root node_modules so packages that are imported
# directly but are only transitive deps (e.g. `express`, pulled in by
# @nestjs/platform-express) resolve at runtime. Matches the dev environment and
# is required for the production Docker build's filtered install.
shamefully-hoist=true
